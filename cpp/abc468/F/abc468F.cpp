#include <bits/stdc++.h>

#define ll long long
#define ld long double

using namespace std;

int n;

void solve() {
    cin>>n;
    vector<int> p(n+1, 0);
    for(int i=1; i<=n ; i++) {
        cin>>p[i];
    }
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    solve();
}